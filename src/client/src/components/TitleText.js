import React from 'react'

const TitleText = (props) => {

    return (
        <>
            <h1 style={{margin: "20px", fontSize: "70px", color: "#006EE2"}}>{props.text}</h1>
        </>
    )
}

export default TitleText